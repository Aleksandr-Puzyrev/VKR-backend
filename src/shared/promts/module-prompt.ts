import { CreateModuleCourseDto } from "src/resources/ai/dto/create-module-course.dto";

export const modulePrompt = (body: CreateModuleCourseDto) => {
  const prompt = `Ты — эксперт по разработке учебных курсов. Создай подробный контент для одного модуля на основе: ${body}.
    Формат ответа — строго в JSON:
      {
        "title": "Название урока",
        "content": "Подробный текст урока с пояснениями и примерами кода, если необходимо",
        "questions": [
          {
            "question": "Формулировка тестового вопроса",
            "options": ["A", "B", "C", "D"],
            "answer": "Правильный вариант (например, A)"
          }
        ]
      }
      Если в модуле несколько уроков, повтори структуру массива lessons: [...] и сгенерируй для каждого урока внутри модуля, например:

      json
      Копировать
      Редактировать
      {
        "module": "Название модуля",
        "lessons": [
          {
            "title": "Урок 1",
            "content": "Текст",
            "questions": [...]
          },
          {
            "title": "Урок 2",
            "content": "Текст",
            "questions": [...]
          }
        ]
      }
    Обрати внимание:

    Контент должен быть подробным, рассчитанным на самостоятельное изучение (около 5–15 минут чтения на урок).

    Примеры кода и пояснения — обязательны для технических тем.

    В конце каждого урока добавляй 1–3 тестовых вопроса (можно с пояснением к правильному ответу в поле "explanation" — если хочешь дополнительно).

    ❗ Вводить нужно: название модуля или название урока из структуры курса.
    ❗ Ответ — только в JSON, без комментариев и описаний снаружи.`;
  return prompt;
};
